# Tasks

このディレクトリでは、未確定事項、実装候補、マイルストーンを管理する。

仕様として確定した内容は、ここではなく `docs/product/`, `docs/design/`, `docs/api/`, `docs/prompts/` の該当ファイルへ反映する。

## 使い分け

- 仕様として実装してよい内容: `docs/` の該当仕様へ書く。
- まだ検討中の論点: `docs/tasks/` に置くか、該当仕様内に `検討中` として書く。
- MVP 対象外だが残す論点: `保留` または `MVP対象外` と明記する。
